import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-t-villaganas',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
